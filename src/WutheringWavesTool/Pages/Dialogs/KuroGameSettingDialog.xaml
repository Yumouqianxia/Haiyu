<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Haiyu.Pages.Dialogs.KuroGameSettingDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ani="using:CommunityToolkit.WinUI.Animations"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:Haiyu.Pages.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
    CornerRadius="5"
    mc:Ignorable="d">
    <i:Interaction.Behaviors>
        <i:EventTriggerBehavior EventName="Loaded">
            <i:InvokeCommandAction Command="{x:Bind ViewModel.LoadedCommand}" />
        </i:EventTriggerBehavior>
    </i:Interaction.Behaviors>
    <Grid RowSpacing="5">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <RepositionThemeTransition />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock
            HorizontalAlignment="Center"
            FontFamily="{ThemeResource DefaultFont}"
            FontSize="18"
            Text="游戏设置" />
        <Border
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,15,0,0"
            Padding="15,10"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="5">

            <NumberBox Header="下载速度限制" Value="{x:Bind ViewModel.DownloadSpeedLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Border>
        <Grid
            Grid.Row="2"
            Padding="15,5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="5"
            Visibility="{x:Bind ViewModel.WavesSettingVisibility, Mode=OneWay}">
            <ani:Implicit.ShowAnimations>
                <ani:OpacityAnimation To="1" Duration="0:0:0.5" />
            </ani:Implicit.ShowAnimations>
            <ani:Implicit.HideAnimations>
                <ani:OpacityAnimation To="1" Duration="0:0:0.5" />
            </ani:Implicit.HideAnimations>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Directx11 启动" />

            <Border
                Grid.Column="1"
                Width="30"
                MaxWidth="30"
                HorizontalAlignment="Right">
                <CheckBox
                    Margin="5"
                    VerticalAlignment="Center"
                    HorizontalContentAlignment="Right" />
            </Border>
            <NavigationViewItemSeparator Grid.Row="1" Grid.ColumnSpan="2" />
            <TextBlock
                Grid.Row="2"
                VerticalAlignment="Center"
                Text="禁用Dlss" />
            <Border
                Grid.Row="2"
                Grid.Column="1"
                Width="30"
                MaxWidth="30"
                HorizontalAlignment="Right">
                <CheckBox
                    Margin="5"
                    VerticalAlignment="Center"
                    HorizontalContentAlignment="Right" />
            </Border>
        </Grid>
        <Grid
            Grid.Row="3"
            Padding="15,15"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="5">
            <TextBox Header="启动参数" />
        </Grid>
        <Button
            Grid.Row="4"
            Margin="0,10,0,0"
            HorizontalAlignment="Center"
            Command="{x:Bind ViewModel.CloseCommand}"
            Content="保存" />
    </Grid>
</ContentDialog>
