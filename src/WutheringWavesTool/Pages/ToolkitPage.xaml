<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Haiyu.Pages.ToolkitPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Haiyu.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:Haiyu.Controls"
    mc:Ignorable="d">

    <Grid
        Margin="5"
        ColumnSpacing="5"
        RowSpacing="5">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromVerticalOffset="50" />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid
            Margin="25"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"
            BorderThickness="1"
            CornerRadius="5">

            <Grid
                Padding="7"
                ColumnSpacing="8"
                CornerRadius="5"
                RowSpacing="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Border
                    Grid.RowSpan="2"
                    Padding="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="{ThemeResource AppBarBackgroundThemeBrush}"
                    CornerRadius="5">
                    <ui:ImageEx
                        Width="45"
                        Height="45"
                        Source="/Assets/kuroRobot.png" />
                </Border>
                <StackPanel
                    Grid.Column="1"
                    Margin="0,5,0,0"
                    Orientation="Horizontal">

                    <TextBlock
                        FontFamily="{ThemeResource DefaultFont}"
                        FontSize="15"
                        FontWeight="Bold"
                        Text="库街区Token读取" />
                </StackPanel>
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    FontSize="13"
                    Foreground="{ThemeResource SystemColorGrayTextBrush}"
                    Text="本工具通过ADB Port调试端口，链接安卓系统的WebView，并通过CDP调用协议抓取Token信息"
                    TextWrapping="Wrap" />
                <Button
                    Grid.Row="2"
                    Grid.Column="2"
                    HorizontalAlignment="Right"
                    Command="{x:Bind ViewModel.ShowAutoKuroTokenCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon
                            FontFamily="Segoe Fluent Icons"
                            FontSize="13"
                            Glyph="&#xE8A7;" />
                        <TextBlock FontSize="13" Text="打开" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Page>
